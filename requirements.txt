# Leaffliction — dependencies (Python >=3.11,<3.12)
# Install: pip install -r requirements.txt
#
# Binder: prompt_toolkit/asttokens are NOT listed here to avoid pip resolving
# a broken conda path (/home/conda/feedstock_root/.../asttokens.../work). The
# Binder image provides a working Jupyter kernel. If you see "No module named
# 'prompt_toolkit.cursor_shapes'" in the kernel, run in a cell:
#   !pip install --quiet 'prompt_toolkit>=3.0.48'

# Core ML / vision
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0,<3
opencv-python>=4.8.0

# Augmentation & transforms (drop-in: import albumentations)
# This project uses albumentationsx only. If you see "albucore incompatible" with albumentations,
# uninstall the old package:  pip uninstall albumentations
albumentationsx>=2.0.13

# Plant / leaf analysis
plantcv>=4.0
rembg>=2.0

# Plotting & CLI
matplotlib>=3.7.0
